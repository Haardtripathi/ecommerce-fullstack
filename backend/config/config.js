const config = {
    FRONTEND_URL: process.env.FRONTEND_URL || 'https://ecomm-fullstack-demo1.netlify.app',
    BACKEND_URL: process.env.BACKEND_URL || 'https://ecommerce-fullstack-1-dsid.onrender.com',
    NODE_ENV: process.env.NODE_ENV || 'development'
};

module.exports = config;